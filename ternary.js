/*
 * Dibawah ini terdapat sebuah variabel bernama result
 * Masukkan lah sebuah nilai ke dalam variabel result tersebut dengan kondisi
 * - Jika nilai variabel harga lebih dari atau sama dengan 100 maka nilai result akan berisi string:
 *   Harga Mahal
 * - Sementara jika harga dibawah dari 100 maka nilai result akan berisi string:
 *   Harga Murah
 * Tampilkan Variabel Tersebut pada terminal
 * PERHATIKAN: UNTUK MENERAPKAN CODINGAN DIATAS HARAP MENGGUNAKAN TERNARY OPERATOR
 */

var harga = 100;

var result = (harga >= 100) ? "Harga Mahal" : "Harga Murah";

console.log(result);
